services:
  # Backend API
  - type: web
    name: eleicao-api
    env: node
    plan: free
    buildCommand: |
      cd apps/api
      npm cache clean --force
      rm -rf node_modules package-lock.json
      npm install --no-package-lock --no-audit --no-fund --legacy-peer-deps --force
      npm run build
    startCommand: cd apps/api && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
      - key: SUPABASE_URL
        value: "SUA_URL_DO_SUPABASE_AQUI"
      - key: SUPABASE_ANON_KEY
        value: "SUA_CHAVE_ANONIMA_AQUI"
      - key: SUPABASE_SERVICE_ROLE_KEY
        value: "SUA_CHAVE_SERVICE_ROLE_AQUI"

  # Frontend Admin - Versão Básica
  - type: web
    name: eleicao-admin
    env: static
    plan: free
    buildCommand: |
      cd apps/admin
      cp package-basic.json package.json
      npm cache clean --force
      rm -rf node_modules package-lock.json dist
      npm install --no-package-lock --no-audit --no-fund --legacy-peer-deps --force
      npm run build
    staticPublishPath: apps/admin/dist
    envVars:
      - key: VITE_API_URL
        value: https://eleicao-api.onrender.com
      - key: NODE_VERSION
        value: 18

  # Frontend Web - Versão Básica
  - type: web
    name: eleicao-web
    env: static
    plan: free
    buildCommand: |
      cd apps/web
      cp package-basic.json package.json
      npm cache clean --force
      rm -rf node_modules package-lock.json dist
      npm install --no-package-lock --no-audit --no-fund --legacy-peer-deps --force
      npm run build
    staticPublishPath: apps/web/dist
    envVars:
      - key: VITE_API_URL
        value: https://eleicao-api.onrender.com
      - key: NODE_VERSION
        value: 18
